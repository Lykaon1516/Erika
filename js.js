document.addEventListener('DOMContentLoaded', () => {
    const boton = document.getElementById('btnIngresar');

    boton.addEventListener('click', () => {
        alert('Iniciando sesión...');
        // Aquí puedes poner: window.location.href = "otra_pagina.html";
    });
});